'use strict'
/**
 * Add two numbers
 * @param {Number} a First number
 * @param {Number} b  Second Number
 * @returns {Number} Total result
 */
function add(a, b) {
    return a + b;
}

/**
 * Add two numbers and return a string
 * @param {Number} a First number
 * @param {Number} b  Second Number
 * @returns {string} Total result
 */
const addTwo = (a, b) => `The result is: ${a + b}`;